<div class="header-spacer"></div>
<section class="single-page">
  <div class="inner">
    <div class="page-title">
      <h1 class="section-title">{{ page.title }}</h1>
    </div>
    <div class="sp-content">{{ page.content }}</div>
  </div>
</section>